
import DashboardClientPage from './dashboard-client-page';

// This is now a simple wrapper. All data fetching is handled in the layout.
export default async function DashboardPage() {
  return <DashboardClientPage />;
}
